#include "appGlobals.h"

// Define global variables declared in appGlobals.h
int motionVal = 0;          // Motion detection value
int colorDepth = 0;         // Camera color depth setting
bool nightSwitch = false;   // Night mode switch
int lightLevel = 0;         // Light level for night mode detection